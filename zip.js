
function zip(arr1, arr2) {
    let zipped = new array[][];

    let len = arr1.length();

    // [1, 2, 3] [4, 5, 6]

    for(let i = 0; i < len; i++) {
        zipped[0][i] = arr1[i]; 
        zipped[1][i] = arr2[i];
    }
    return zipped;
}
